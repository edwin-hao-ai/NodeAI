---
description: NodeAI 产品架构、双路径与核心决策（PRD 口径）
alwaysApply: true
---

# NodeAI 产品规则

## 定位

桌面 **AI Token 用量中枢** — **选模型**为首屏（VPN 式切换）；Chat 在「更多」，是快捷入口而非产品全部。核心价值：谁在用、花了多少、默认省钱、跨 App 记忆。

## 双路径（不可混写）

| 路径 | 流量 | NodeAI 服务器 |
|------|------|---------------|
| **含额度** | 8787 边缘 → NodeAI Cloud API → Vercel Gateway | ✅ 鉴权/计费/转发 |
| **自有 Key** | 8787 → 桌面 Runtime → 用户模型服务商 | ❌ 零推理/中转 |

- 自有 Key 的 API Key **仅 Keychain，不上云**
- 混合 Fallback（自有 Key 失败回落含额度）**默认关闭**，须二次确认
- 零登录自有 Key：可不注册，仅用本地 Key + 8787

## Built-in Bonus 优先级（MVP）

默认 ON：智能压缩(RTK)、简洁回复(Caveman L1)、智能选模型、限流自动换路、记忆注入、格式转换(不可见)。

粘性优先叙事：**按 App 账单、实时面板、不断工、记忆** > 单独强调压缩算法。

## 技术栈

- 桌面：Tauri + Rust 代理核心；UI 可 Web 技术
- 含额度上游：Vercel AI Gateway（Phase 1）
- Memory：默认本地 SQLite

## 激活与成长

- 小美激活 = **首次 Chat 成功**（非必须连 Cursor）
- L0–L1 默认 UI；连接 IDE / 自有 Key 在「连接」「模型来源」可发现，不挡 onboarding

## 文档来源

实现前对照 `docs/PRD.md` §3.4（用户文案）、§3.7（Built-in Bonus）、§5.12（自有 Key）。
