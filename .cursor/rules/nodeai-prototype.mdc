---
description: NodeAI HTML 原型结构与设置页约定
globs: prototypes/**
alwaysApply: false
---

# NodeAI 原型规则

## 文件

- `prototypes/auth.html` — 登录/注册/Onboarding
- `prototypes/dashboard.html` — 主应用（选模型首屏 + 侧栏导航）

预览：`open prototypes/dashboard.html` 或 `?mode=local`（零登录自有 Key）

## 信息架构

- 主 nav：**选模型 / 对话 / 总览 / 连接**（对话为二级入口，仍在主导航；记忆/账单等在「更多」）
- 默认视图 `view-models`：**单列 VPN 式**（盾牌状态 + 线路列表 + 底部智能开关）；对话页线路只读 chip，点击「去换」回选模型页
- 侧栏「新对话 / 历史」仅在 **对话** 视图显示（`sidebar-chat-mode`）
- **对话页专注聊天**：单列消息 + 输入框；顶栏仅「当前线路」+ 轻量「用量 → 总览」入口
- **实时用量**在 **总览顶部**（`hub-live-block`）与 **菜单栏托盘**（sparkline + 应用 mini）；不在对话页占右侧栏
- Hub **内置能力** 卡片网格 + 账单 **怎么省的** 四格（压缩/简洁/选模型/合计）

## 设置页分组（应对齐 PRD BonusProfile）

1. **通用** — 界面语言
2. **账号** — 邮箱/套餐、登录开通含额度（本地模式）、退出
3. **连接** — 本地端口（默认 8787，改后提示同步 Cursor）
4. **对话与偏好** — 默认回复语言、跨应用记忆、允许 Cursor 写入记忆
5. **路由与转换** — 智能选模型、选模型时可用我的来源、限流自动换路、自有 Key 失败时用含额度（默认关）、格式自动转换
6. **省钱** — 智能压缩、简洁回复（主区）、预算告警；**高级** — Agent 文件、简洁强度、上下文整理(v0.2)、基础设施状态

## 双路径 UI

- 模型菜单 / 路径徽章：`含额度` vs `本地`（不说 BYOK）
- 账单 Tab：全部 / 含额度·云端 / 我的 Key·本地
- 模型来源页：含额度来源 + 添加自有来源 + 隐私承诺 + 分路径自动换路说明

## 样式

遵循 `DESIGN.md` M3 dark tokens；省钱色 `--savings`；不引入新框架依赖。
